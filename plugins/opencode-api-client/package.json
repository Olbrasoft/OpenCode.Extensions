{
  "name": "opencode-api-client",
  "version": "1.0.0",
  "description": "OpenCode plugin that connects to our custom C# API for session and message logging",
  "main": "dist/index.js",
  "type": "module",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "opencode",
    "plugin",
    "session",
    "logging",
    "postgresql"
  ],
  "author": "Olbrasoft",
  "license": "MIT",
  "dependencies": {
    "@opencode-ai/plugin": "^1.0.122"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
